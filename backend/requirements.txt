# ARA MetaboliQ Backend — Python Dependencies
# Phase 4: Voice AI Module

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0

# Voice AI — Feature extraction
opensmile>=2.5.0
numpy>=1.26.0

# Voice AI — Classification (hackathon)
scikit-learn>=1.5.0
xgboost>=2.1.0
joblib>=1.4.0

# Voice AI — Full product (HuBERT)
# transformers>=4.44.0
# torch>=2.4.0

# Voice AI — Full product (ONNX inference)
# onnxruntime>=1.19.0

# Database (Phase 19 — Sonnet)
# sqlalchemy[asyncio]>=2.0
# asyncpg>=0.29
# alembic>=1.13

# Auth (Phase 20 — Opus security)
# python-jose[cryptography]>=3.3
# passlib[bcrypt]>=1.7
# argon2-cffi>=23.1

# Redis (Phase 19)
# redis>=5.0
